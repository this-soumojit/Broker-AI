NODE_ENV=development

# Database configurations (Update with your actual values)
DB_HOST=127.0.0.1
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=your_db_password
DB_NAME=your_db_name

# SMTP configurations (Update with your email service)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_email@gmail.com
SMTP_PASSWORD=your_app_password
SMTP_FROM_EMAIL=your_email@gmail.com
SMTP_FROM_NAME="Your App Name"
SMTP_SECURE=false

# JWT configurations (Change this to a secure random string)
JWT_SECRET=your_super_secret_jwt_key_here_make_it_long_and_random

# Allowed origins
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173

# Swagger configurations
SWAGGER_HOST=http://localhost:8000

# ===== INVOICE EXTRACTION CONFIGURATION =====

# Google Document AI (Optional - for enterprise-grade extraction)
# Leave empty to use free pattern matching
GOOGLE_APPLICATION_CREDENTIALS=./credentials/google-document-ai-key.json
GOOGLE_PROJECT_ID=freeinvoiceparser
GOOGLE_LOCATION=us
DOCUMENT_AI_PROCESSOR_ID=

# OpenAI (Optional - for AI-powered extraction)
# Leave empty to use free pattern matching
OPENAI_API_KEY=

# ===== END INVOICE EXTRACTION =====
